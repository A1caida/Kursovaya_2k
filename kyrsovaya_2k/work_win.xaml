<Window x:Class="kyrsovaya_2k.work_win"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kyrsovaya_2k"
        mc:Ignorable="d"
        Title="Библиотека" Height="450" Width="800" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.4*"></RowDefinition>
            <RowDefinition Height="0.4*"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="0.4*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TabControl Grid.ColumnSpan="2" Grid.RowSpan="5" SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="Общее">
                <TextBlock x:Name="name" FontSize="30" Grid.ColumnSpan="2" Grid.Row="1" />
                
            </TabItem>
            <TabItem Header="Авторы">
                <StackPanel>
                    <Button x:Name="s_auth" Click="search_auth" Content="Поиск" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120" Height="33" Margin="0,10,10,10" />
                    <DataGrid x:Name="authors" ItemsSource="{Binding}" AutoGenerateColumns="True" Grid.ColumnSpan="2" Grid.Row="2" Grid.RowSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Height="242" Width="774" RowHeight="16" SelectedValuePath="id" />
                </StackPanel>
            </TabItem>
            
        </TabControl>
       

    </Grid>
</Window>
